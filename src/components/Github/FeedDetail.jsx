import React, { Text, View } from 'react-native';

export default class FeedRow extends React.Component {
    static propTypes = {
    }

    render() {
        return (
            <View>
                <Text>
                    Feed detail ...
                </Text>
            </View>
        );
    }
}